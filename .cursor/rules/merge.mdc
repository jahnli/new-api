---
description: 合并上游仓库 (upstream) 的严格流程——二开项目，冲突必须逐一确认
alwaysApply: false
---

# 合并上游仓库 (Upstream Merge) 规范

本项目是的二开 fork，已大量修改。合并上游时必须极其谨慎，**绝不允许自动解决冲突或静默覆盖本地改动**。

## 0. 前置检查（必须全部通过才能开始）

1. **工作区干净**：`git status` 确认无未提交改动。如有，先 stash 或 commit。
2. **确认目标**：明确告知用户将要合并的上游分支（默认 `upstream/main`）和对应的本地分支（默认 `main`），获得用户确认后才可继续。
3. **Fetch 上游**：`git fetch upstream` 拉取最新上游提交。
4. **查看差异规模**：运行 `git log --oneline main..upstream/main` 和 `git diff --stat main..upstream/main` 展示给用户，让用户了解此次合并的范围和规模。
5. **备份分支**：创建备份分支 `git branch backup/pre-merge-<日期YYYYMMDD> main`，告知用户备份分支名称。

## 1. 合并策略

- 优先以本地为主，确保本地功能不丢失
- 合并时不要恢复我删除掉的功能
   - 不需要个人设置的账号管理，其他设置中的价格设置和隐私设置
   - 不合并用户管理中的用户名称查询的功能
- **必须使用** `git merge upstream/main --no-commit --no-ff`（不自动提交、不快进），给予充分的审查空间。
- **禁止** `git merge --squash`、`git rebase`、`git reset --hard` 等破坏性操作，除非用户明确要求。
- **禁止** 在未经用户确认的情况下运行 `git merge --continue` 或 `git commit`。

## 2. 冲突处理——逐文件确认

合并后立即运行 `git diff --name-only --diff-filter=U` 获取冲突文件列表。

### 对每个冲突文件，必须执行以下流程：

1. **展示冲突上下文**：使用 `git diff <file>` 显示冲突标记（`<<<<<<<`、`=======`、`>>>>>>>`），清晰标注哪些是"我们的改动 (ours)"、哪些是"上游改动 (theirs)"。
2. **解释差异**：用中文向用户解释：
   - 本地版本做了什么（二开功能/修改）
   - 上游版本做了什么（新功能/修复/重构）
   - 两者的冲突点在哪里
3. **提供选项**：使用 AskQuestion 工具让用户选择：
   - **保留本地 (ours)**：保持二开的改动不变
   - **采用上游 (theirs)**：使用上游的新版本
   - **手动合并**：由 AI 提出合并建议，展示合并后的完整代码，用户确认后再写入
4. **等待确认**：用户明确选择后才可修改该文件。**绝不可跳过任何一个冲突文件**。
5. **标记已解决**：用户确认后，`git add <file>` 将该文件标记为已解决。

### 特别注意的高风险区域（二开重点改动区）：

以下文件/目录在二开中有大量自定义改动，冲突时需要**格外仔细**地逐行审查：

- `controller/` — 自定义 controller 逻辑（用户管理、LDAP 等）
- `model/` — 数据模型扩展（用户统计、订阅配额等）
- `service/` — 自定义业务逻辑
- `middleware/` — 自定义中间件
- `web/src/` — 前端组件和页面的自定义改动
- `web/src/i18n/locales/` — 国际化文件（有自定义 key）
- `router/` — 自定义路由
- `setting/` — 自定义配置

## 3. 非冲突文件审查

即使文件没有冲突，也需要审查上游的改动是否会影响二开功能：

1. 运行 `git diff --cached --name-only` 列出所有已自动合并的文件。
2. 对涉及核心逻辑（controller、model、service、relay、middleware）的文件，简要向用户说明上游做了什么改动。
3. 如果上游改动可能与二开功能产生**语义冲突**（无 git 冲突标记但逻辑上不兼容），必须提醒用户并请求确认。

## 4. 提交前验证

所有冲突解决后、commit 之前，必须完成：

1. **编译检查**（后端）：`go build ./...` 确保 Go 代码编译通过。
2. **前端检查**：`cd web && bun run build` 确保前端构建通过。
3. **展示最终改动摘要**：`git diff --cached --stat` 展示将要提交的所有改动概况。
4. **获得最终确认**：明确问用户"以上是本次合并的所有改动，确认提交吗？"，用户确认后才可 commit。

## 5. 提交

- commit message 格式：`merge: sync upstream/main up to <上游最新commit短hash>`
- commit 后运行 `git log --oneline -5` 展示给用户确认。
- **不要自动 push**，等用户指示。

## 6. 合并失败/中止

如果合并过程中用户决定中止：

1. `git merge --abort` 回退到合并前状态。
2. 确认工作区已恢复干净：`git status`。
3. 告知用户备份分支仍然存在，可随时重试。

## 7. 绝对禁止事项

- ❌ 未经用户确认就解决任何冲突
- ❌ 静默选择 ours 或 theirs
- ❌ 跳过冲突文件不处理
- ❌ 在用户未确认的情况下 commit 或 push
- ❌ 使用 `--force` 相关的任何操作
- ❌ 修改或删除受保护的项目信息（见 Rule 5: Protected Project Information）
- ❌ 自动合并后不做编译/构建验证
