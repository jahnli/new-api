---
description: 项目约定与编码规范
alwaysApply: true
---

# 项目约定

## 概述

本项目是一个用 Go 编写的 AI API 网关/代理。它在统一 API 之后聚合 40+ 上游 AI 提供商（OpenAI、Claude、Gemini、Azure、AWS Bedrock 等），并提供用户管理、计费、限流与管理后台。

## 技术栈

- **后端**：Go 1.22+、Gin Web 框架、GORM v2 ORM
- **前端**：React 18、Vite、Semi Design UI（@douyinfe/semi-ui）
- **数据库**：SQLite、MySQL、PostgreSQL（须同时支持三者）
- **缓存**：Redis（go-redis）+ 内存缓存
- **认证**：JWT、WebAuthn/通行密钥、OAuth（GitHub、Discord、OIDC 等）
- **前端包管理器**：Bun（优先于 npm/yarn/pnpm）

## 架构

分层架构：Router → Controller → Service → Model

```
router/        — HTTP 路由（API、relay、dashboard、web）
controller/    — 请求处理
service/       — 业务逻辑
model/         — 数据模型与数据库访问（GORM）
relay/         — AI API 转发/代理及提供商适配器
  relay/channel/ — 各提供商适配器（openai/、claude/、gemini/、aws/ 等）
middleware/    — 认证、限流、CORS、日志、分发等
setting/       — 配置管理（ratio、model、operation、system、performance）
common/        — 共享工具（JSON、crypto、Redis、env、rate-limit 等）
dto/           — 数据传输对象（请求/响应结构体）
constant/      — 常量（API 类型、渠道类型、上下文键等）
types/         — 类型定义（relay 格式、文件来源、错误等）
i18n/          — 后端国际化（go-i18n，en/zh）
oauth/         — OAuth 提供商实现
pkg/           — 内部包（cachex、ionet）
web/           — React 前端
  web/src/i18n/  — 前端国际化（i18next，zh/en/fr/ru/ja/vi）
```

## 国际化（i18n）

### 后端（`i18n/`）

- 库：`nicksnyder/go-i18n/v2`
- 语言：en、zh

### 前端（`web/src/i18n/`）

- 库：`i18next` + `react-i18next` + `i18next-browser-languagedetector`
- 语言：zh（回退）、en、fr、ru、ja、vi
- 翻译文件：`web/src/i18n/locales/{lang}.json` — 扁平 JSON，键为中文源字符串
- 用法：在组件中使用 `useTranslation()` 钩子，调用 `t('中文key')`
- Semi UI 语言环境通过 `SemiLocaleWrapper` 同步
- CLI：`bun run i18n:extract`、`bun run i18n:sync`、`bun run i18n:lint`

## 规则

### 规则 1：JSON 封装 — 使用 `common/json.go`

所有 JSON 序列化/反序列化**必须**使用 `common/json.go` 中的封装函数：

- `common.Marshal(v any) ([]byte, error)`
- `common.Unmarshal(data []byte, v any) error`
- `common.UnmarshalJsonStr(data string, v any) error`
- `common.DecodeJson(reader io.Reader, v any) error`
- `common.GetJsonType(data json.RawMessage) string`

业务代码中**不要**直接 import 或调用 `encoding/json`。这些封装用于一致性与后续可扩展性（例如可替换为更快的 JSON 库）。

说明：`json.RawMessage`、`json.Number` 等来自 `encoding/json` 的类型定义仍可作类型引用，但实际 marshal/unmarshal 调用须走 `common.*`。

### 规则 2：数据库兼容性 — SQLite、MySQL >= 5.7.8、PostgreSQL >= 9.6

所有数据库相关代码**必须**在三种数据库上同时完全兼容。

**使用 GORM 抽象：**

- 优先使用 GORM 方法（`Create`、`Find`、`Where`、`Updates` 等），少用裸 SQL。
- 主键生成交给 GORM，不要直接使用 `AUTO_INCREMENT` 或 `SERIAL`。

**无法避免裸 SQL 时：**

- 列名引号不同：PostgreSQL 用 `"column"`，MySQL/SQLite 用 `` `column` ``。
- 对 `group`、`key` 等保留字列，使用 `model/main.go` 中的 `commonGroupCol`、`commonKeyCol`。
- 布尔值不同：PostgreSQL 用 `true`/`false`，MySQL/SQLite 用 `1`/`0`。使用 `commonTrueVal`/`commonFalseVal`。
- 使用 `common.UsingPostgreSQL`、`common.UsingSQLite`、`common.UsingMySQL` 分支数据库特有逻辑。

**无跨库回退时禁止：**

- 仅 MySQL 的函数（例如无 PostgreSQL `STRING_AGG` 等价物时使用 `GROUP_CONCAT`）
- 仅 PostgreSQL 的运算符（例如 `@>`、`?`、`JSONB` 运算符）
- SQLite 中的 `ALTER COLUMN`（不支持 — 使用加列变通方案）
- 无回退的数据库专有列类型 — JSON 存储用 `TEXT` 而非 `JSONB`

**迁移：**

- 确保所有迁移在三种数据库上均可运行。
- SQLite 使用 `ALTER TABLE ... ADD COLUMN` 而非 `ALTER COLUMN`（模式见 `model/main.go`）。

### 规则 3：前端 — 优先 Bun

前端（`web/` 目录）优先使用 `bun` 作为包管理与脚本运行器：

- `bun install` 安装依赖
- `bun run dev` 开发服务器
- `bun run build` 生产构建
- `bun run i18n:*` 国际化工具链

### 规则 4：新渠道的 StreamOptions 支持

实现新渠道时：

- 确认上游是否支持 `StreamOptions`。
- 若支持，将该渠道加入 `streamSupportedChannels`。

### 规则 5：上游 Relay 请求 DTO — 保留显式零值

对从客户端 JSON 解析、再序列化发给上游提供商的请求结构体（尤其 relay/convert 路径）：

- 可选标量字段**必须**使用带 `omitempty` 的指针类型（如 `*int`、`*uint`、`*float64`、`*bool`），不得使用非指针标量。
- 语义**必须**为：
  - 客户端 JSON 中缺省该字段 => `nil` => 序列化时省略；
  - 显式设为 0/false => 非 `nil` 指针 => **仍须**发给上游。
- 可选请求参数不要用非指针标量配 `omitempty`，否则零值（`0`、`0.0`、`false`）会在序列化时被静默丢弃。

### 规则 6：构建与验证 — 勿在每次改完后例行全量 build

**不要**在每次帮我修改后都跑一遍后端全量构建与前端生产构建（例如根目录/`web/` 下的 `go build`、`bun run build` / `npm run build` 等完整产物构建）。
